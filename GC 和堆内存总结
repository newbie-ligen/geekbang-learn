垃圾回收期分别采用串行、并行、cms、g1，内存512m、1g、2g、4g,
内存增大gc频率下降，单次gc时间边长，内存不是越大越好，内存越大会导致单次gc时间变长，从而导致gc时系统响应慢
内存在512时，不管yong gc和full gc都很频繁，内存不能过小，过小会导致，gc频繁，从而大量的资源用于垃圾回收
内存大小一致时串行gc，单次gc时长是并行gc的数倍，并行gc回收效率比串行高
内存越大G1的回收效率越高，到2g，4g回收时间最短

